# VOLQ Daemon (volqd)

volqd is the daemon component of VOLQ Panel.

## Installation

1. Clone the repository
2. Run npm install
3. Configure config.json with your access key
4. Start with npm start or pm2 start index.js

## Overview

The daemon runs as a background service, interfacing with VOLQ Panel for operational commands and status updates.
